#include<bits/stdc++.h>
using namespace std;

  void rev(int l,int h,int arr[])
 {
    if(l>h)
    return;
    
    swap(arr[l],arr[h]);
    rev(l+1,h-1,arr);
 }
int main()
{
 int a[4]={1,2,3,4};
 
  rev(0,3,a);
  
  for(int i=0;i<4;i++)
  {
      cout<<a[i];
  }
  
  return 0;
  
}
